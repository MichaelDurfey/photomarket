# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Runtime JSON under data/ (users, tokens, optional photos)
data/
# Legacy token path (still read by adobe-lightroom.js); never commit secrets
adobe-tokens.json

# SSL certs and keys (keep outside repo; set SSL_KEY_PATH/SSL_CERT_PATH in .env)
certs/

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

